/**
 * 桌面数据
 */
export default class TableData{
    id = null;
    type =null;
    static type_defaule = 1; // 默认桌面
    static type_children = 2; // 子桌面
    name = null; // 桌面名称
    allBlock = []; // 全部图标
}